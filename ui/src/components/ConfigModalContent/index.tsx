import React from "react";
import styled from "styled-components";

const ConfigModalContentWrapper = styled.div`
  p {
    color: inherit;
  }
`;

export const ConfigModalContent: React.FC = () => (
  <ConfigModalContentWrapper>

  </ConfigModalContentWrapper>
);
